# Database Production - Render PostgreSQL
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/titan_prod}
spring.datasource.username=${DATABASE_USERNAME:titan_user}
spring.datasource.password=${DATABASE_PASSWORD:titan_password}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Production optimisé pour Render
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.leak-detection-threshold=60000

# JPA Production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# MinIO Configuration Production (Service Render interne)
# Render utilise des URLs internes pour la communication inter-services
minio.url=${MINIO_URL:http://titan-minio:9000}
minio.access-key=${MINIO_ACCESS_KEY:admin}
minio.secret-key=${MINIO_SECRET_KEY:password123}
minio.bucket.songs=${MINIO_BUCKET_SONGS:titan-songs-prod}
minio.bucket.images=${MINIO_BUCKET_IMAGES:titan-images-prod}
minio.bucket.videos=${MINIO_BUCKET_VIDEOS:titan-videos-prod}
minio.bucket.photos=${MINIO_BUCKET_PHOTOS:titan-photos-prod}

# JWT Configuration Production
jwt.jwtSecret=${JWT_SECRET:2b44b0b00fd822d8ce753e54dac3dc4e06c2725f7db930f3b9924468b53194dbccdbe23d7baa5ef5fbc414ca4b2e64700bad60c5a7c45eaba56880985582fba4}
jwt.jwtExpirationMs=${JWT_EXPIRATION_MS:86400000}

# Email Configuration Production
# IMPORTANT: Configure MAIL_USERNAME and MAIL_PASSWORD manually on Render Dashboard
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging Production
logging.level.root=WARN
logging.level.nitchcorp.backend.titan=INFO
logging.level.io.minio=WARN
logging.level.org.springframework.web=WARN

# Actuator pour health check Render
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.db.enabled=true
